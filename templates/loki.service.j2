[Unit]
Description=Loki service
After=network.target

[Service]
Type=simple
ExecStart={{ loki_install_dir }}/loki-linux-amd64 -config.file {{ loki_config_dir }}/config-loki.yml

[Install]
WantedBy=multi-user.target